
    
    
(letrec ((is-even? (lambda (n)(if (= 0 n) #t (is-odd? (- n 1)))))
           (is-odd? (lambda (n) (if (= 0 n) #f  (is-even? (- n 1))))))
    (is-odd? 3))
    
    
(define (fact i acc) (write i) (write acc) 
      (if( = i 0) acc  (fact (1- i) (* acc i))))
          
          
    (define (mymax nums)                    
  (cond ((= (length nums) 1) (car nums)) 
        ((> (car nums) (cadr nums))      
         (mymax (cons (car nums) 
                      (cddr nums))))     
        (else (mymax (cdr nums)))))        